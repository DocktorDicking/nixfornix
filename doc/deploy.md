# Deploy angular front-end

## Build client application

Build the application with the following command:

> ng build --configuration [client_name]

Change [client_name] to the client's config name.

> ng build --configuration bimmate

> ng build --configuration luca

## Move files to VPS

Connect through SFTP with the VPS and deploy the files to:
> /var/www/html

